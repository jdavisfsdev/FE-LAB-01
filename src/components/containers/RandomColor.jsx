import React, { Component } from 'react';

export default class RandomColor extends Component {
    state = {
      randomColor: 'cyan'
    }

    render() {
      return (
        <div>
                
        </div>
      );
    }
}
